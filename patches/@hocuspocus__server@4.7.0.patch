diff --git a/dist/hocuspocus-server.cjs b/dist/hocuspocus-server.cjs
index 6b46aae..ead50cf 100644
--- a/dist/hocuspocus-server.cjs
+++ b/dist/hocuspocus-server.cjs
@@ -231,10 +231,18 @@ var MessageReceiver = class {
 				} : this.defaultTransactionOrigin ?? { source: "local" };
 				const scratchDoc = new yjs.Doc();
 				const scratch = new y_protocols_awareness.Awareness(scratchDoc);
+				// A scratch decoder has no local participant; do not pass its synthetic state to hooks.
+				scratch.setLocalState(null);
+				scratch.meta.delete(scratchDoc.clientID);
 				try {
 					(0, y_protocols_awareness.applyAwarenessUpdate)(scratch, update, null);
+					// Null entries leave metadata but no state. Retain only those original removals;
+					// a hook deleting a non-null state must still suppress that update, not remove it.
+					const removedClients = [...scratch.meta.keys()].filter(
+						clientId => !scratch.getStates().has(clientId),
+					);
 					await document.callbacks.beforeHandleAwareness(document, scratch.getStates(), origin);
-					update = (0, y_protocols_awareness.encodeAwarenessUpdate)(scratch, [...scratch.getStates().keys()]);
+					update = (0, y_protocols_awareness.encodeAwarenessUpdate)(scratch, [...new Set([...removedClients, ...scratch.getStates().keys()])]);
 				} finally {
 					scratch.destroy();
 					scratchDoc.destroy();
@@ -658,7 +666,7 @@ var Document = class extends yjs.Doc {
 		if (originConnection !== null) {
 			const entry = this.connections.get(originConnection);
 			if (entry) {
-				added.forEach((clientId) => entry.clients.add(clientId));
+				added.concat(updated).forEach((clientId) => entry.clients.add(clientId));
 				removed.forEach((clientId) => entry.clients.delete(clientId));
 			}
 		}
@@ -1510,7 +1518,8 @@ var Hocuspocus = class {
 			});
 		} catch (e) {
 			this.closeConnections(documentName);
-			this.unloadDocument(document);
+			// An unpublished document is outside unloadDocument ownership.
+			document.destroy();
 			throw e;
 		}
 		document.isLoading = false;
@@ -1518,7 +1527,12 @@ var Hocuspocus = class {
 			document.lastChangeTime = Date.now();
 			this.handleDocumentUpdate(document, origin, update);
 		});
-		await this.hooks("afterLoadDocument", hookPayload);
+		try {
+			await this.hooks("afterLoadDocument", hookPayload);
+		} catch (e) {
+			document.destroy();
+			throw e;
+		}
 		document.beforeBroadcastStateless((document, stateless) => {
 			const hookPayload = {
 				document,
diff --git a/dist/hocuspocus-server.esm.js b/dist/hocuspocus-server.esm.js
index d4acd27..c45f9a0 100644
--- a/dist/hocuspocus-server.esm.js
+++ b/dist/hocuspocus-server.esm.js
@@ -201,10 +201,18 @@ var MessageReceiver = class {
 				} : this.defaultTransactionOrigin ?? { source: "local" };
 				const scratchDoc = new Y.Doc();
 				const scratch = new Awareness(scratchDoc);
+				// A scratch decoder has no local participant; do not pass its synthetic state to hooks.
+				scratch.setLocalState(null);
+				scratch.meta.delete(scratchDoc.clientID);
 				try {
 					applyAwarenessUpdate(scratch, update, null);
+					// Null entries leave metadata but no state. Retain only those original removals;
+					// a hook deleting a non-null state must still suppress that update, not remove it.
+					const removedClients = [...scratch.meta.keys()].filter(
+						clientId => !scratch.getStates().has(clientId),
+					);
 					await document.callbacks.beforeHandleAwareness(document, scratch.getStates(), origin);
-					update = encodeAwarenessUpdate(scratch, [...scratch.getStates().keys()]);
+					update = encodeAwarenessUpdate(scratch, [...new Set([...removedClients, ...scratch.getStates().keys()])]);
 				} finally {
 					scratch.destroy();
 					scratchDoc.destroy();
@@ -628,7 +636,7 @@ var Document = class extends Doc {
 		if (originConnection !== null) {
 			const entry = this.connections.get(originConnection);
 			if (entry) {
-				added.forEach((clientId) => entry.clients.add(clientId));
+				added.concat(updated).forEach((clientId) => entry.clients.add(clientId));
 				removed.forEach((clientId) => entry.clients.delete(clientId));
 			}
 		}
@@ -1480,7 +1488,8 @@ var Hocuspocus = class {
 			});
 		} catch (e) {
 			this.closeConnections(documentName);
-			this.unloadDocument(document);
+			// An unpublished document is outside unloadDocument ownership.
+			document.destroy();
 			throw e;
 		}
 		document.isLoading = false;
@@ -1488,7 +1497,12 @@ var Hocuspocus = class {
 			document.lastChangeTime = Date.now();
 			this.handleDocumentUpdate(document, origin, update);
 		});
-		await this.hooks("afterLoadDocument", hookPayload);
+		try {
+			await this.hooks("afterLoadDocument", hookPayload);
+		} catch (e) {
+			document.destroy();
+			throw e;
+		}
 		document.beforeBroadcastStateless((document, stateless) => {
 			const hookPayload = {
 				document,
diff --git a/src/Document.ts b/src/Document.ts
index 9fa5f85..d36deee 100644
--- a/src/Document.ts
+++ b/src/Document.ts
@@ -260,7 +260,8 @@ export class Document extends Doc {
 			const entry = this.connections.get(originConnection);
 
 			if (entry) {
-				added.forEach((clientId: any) => entry.clients.add(clientId));
+				// A state restored after removal retains metadata and is reported as updated.
+				added.concat(updated).forEach((clientId: any) => entry.clients.add(clientId));
 				removed.forEach((clientId: any) => entry.clients.delete(clientId));
 			}
 		}
diff --git a/src/Hocuspocus.ts b/src/Hocuspocus.ts
index 9f9087a..f7c5568 100644
--- a/src/Hocuspocus.ts
+++ b/src/Hocuspocus.ts
@@ -429,7 +429,8 @@ export class Hocuspocus<Context = any> {
 			);
 		} catch (e) {
 			this.closeConnections(documentName);
-			this.unloadDocument(document);
+			// An unpublished document is outside unloadDocument ownership.
+			document.destroy();
 			throw e;
 		}
 
@@ -443,7 +444,12 @@ export class Hocuspocus<Context = any> {
 			},
 		);
 
-		await this.hooks("afterLoadDocument", hookPayload);
+		try {
+			await this.hooks("afterLoadDocument", hookPayload);
+		} catch (e) {
+			document.destroy();
+			throw e;
+		}
 
 		document.beforeBroadcastStateless(
 			(document: Document, stateless: string) => {
diff --git a/src/MessageReceiver.ts b/src/MessageReceiver.ts
index d6b185f..0869f41 100644
--- a/src/MessageReceiver.ts
+++ b/src/MessageReceiver.ts
@@ -87,8 +87,16 @@ export class MessageReceiver {
 				// update entirely.
 				const scratchDoc = new Y.Doc();
 				const scratch = new Awareness(scratchDoc);
+				// A scratch decoder has no local participant; do not pass its synthetic state to hooks.
+				scratch.setLocalState(null);
+				scratch.meta.delete(scratchDoc.clientID);
 				try {
 					applyAwarenessUpdate(scratch, update, null);
+					// Null entries leave metadata but no state. Retain only those original removals;
+					// a hook deleting a non-null state must still suppress that update, not remove it.
+					const removedClients = [...scratch.meta.keys()].filter(
+						clientId => !scratch.getStates().has(clientId),
+					);
 
 					await document.callbacks.beforeHandleAwareness(
 						document,
@@ -97,7 +105,7 @@ export class MessageReceiver {
 					);
 
 					update = encodeAwarenessUpdate(scratch, [
-						...scratch.getStates().keys(),
+						...new Set([...removedClients, ...scratch.getStates().keys()]),
 					]);
 				} finally {
 					scratch.destroy();
