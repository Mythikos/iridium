/**
 * @iridium/editor — CodeMirror 6 + y-codemirror.next binding, markdown language, iridiumFrontmatter block parser, formatting commands, read-only compartment, theme, disposable-view helpers
 *
 * M0 placeholder: the package exists so that the workspace graph, boundaries, knip and the
 * single-instance check exercise the real dependency structure. The package work item of
 * 12-milestones.md section 4.3 replaces this module.
 */
export const packageName = '@iridium/editor' as const;
