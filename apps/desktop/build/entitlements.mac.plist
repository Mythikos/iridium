<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Kept and unused at 1.0: entitlements are meaningful only under a hardened runtime, which in turn is
  meaningful only under a Developer ID signature. The 1.0 macOS bundle is ad-hoc signed
  (07-client-applications.md §7.14.2, §7.14.3), so `mac.hardenedRuntime` is false and nothing here is
  applied. The post-1.0 signing epic sets `hardenedRuntime: true` and this file becomes live without
  being rewritten.
-->
<plist version="1.0">
  <dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
  </dict>
</plist>
