/**
 * @iridium/e2e — Playwright fixtures (collab, frame-counter) land with the milestones that need them
 * (10-testing-and-quality.md, "Playwright 1.63.0 configuration"). M0 placeholder.
 */
export const packageName = '@iridium/e2e' as const;
