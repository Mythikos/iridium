# D06-49: the last-used tracker

Status: accepted 2026-09-25.

**The tracker.** `LastUsedTracker` (`apps/server/src/auth/tokens/last-used.ts`) is in memory and has no database access. It owns one live map from token id to an immutable entry `{at, ip, client, consentId?}` — `consentId` recorded for an OAuth access token — and that map is the only buffer; it never exceeds `TOKEN_LAST_USED_PENDING_MAX` (10 000). `observe()` replaces an existing key's entry when the observation is not older (latest wins) and inserts a new key while the map holds fewer than the bound; at a full map a new key is dropped, counted in `iridium_token_last_used_dropped_total`, and starts an immediate flush unless one is in flight or the last one failed within the current interval, so the triggering observation is never admitted past the bound. `flush()` runs one at a time (a concurrent call joins it), snapshots the map's pairs without removing anything and writes them through the injected sink; after success it deletes each token whose current entry is still the same object as in the snapshot, so an observation that arrived during the flush stays for the next one; after failure it deletes nothing, records the failure time for the backoff rule and logs `auth.token_last_used.flush_failed {pending, droppedSinceSuccess, code}` at warn. Nothing is re-merged, so the bound holds across any sequence of successes and failures. The tracker flushes every `TOKEN_LAST_USED_FLUSH_INTERVAL_MS` (600 000) on the injected `Clock`, with an owned, cancellable timer, in every process that verifies tokens, independent of `JOBS_ENABLED`. `token.revoked` does not drop a pending entry, and `last_client` is `clientLabelOf`'s label (D06-23 as amended).

**The sink.** `auth/tokens/last-used-store.ts` runs each flush through `auth/credential-transaction.ts` at `READ COMMITTED` (A46 as amended): it first advances `oauth_consents.last_used_at` with `GREATEST`, in ascending consent id, then `access_tokens.last_used_at`, `last_used_ip` and `last_client`, monotonically (`WHERE last_used_at IS NULL OR last_used_at < ?`), in ascending token id. It never writes `sessions.last_seen_at`, which stays on `verifySession`'s synchronous throttled path, and it is the only writer of `oauth_consents.last_used_at`.

**Construction and shutdown.** The auth plugin is the one construction site of the tracker and the sink, from `app.clock` and the database handle; it hands the tracker to the `TokenVerifier`, which imports only the `LastUsedObserver` type and calls `observe()` after a successful verification, and registers `tracker.close()` — a final flush, then the timer cancelled — as its `onClose` hook. `auth/plugin.ts` is the only importer of `last-used-store.ts`, so the MCP import closure reaches the tracker's type and never the `UPDATE` (D06-15 as amended). The final flush runs in `onClose`, after every admitted request and before the pools close, under `closeFlushDeadline` within the one `SHUTDOWN_DRAIN_MS` deadline (ARCH-06 as amended); entries it cannot confirm count in `iridium_token_last_used_dropped_total`, and an abandoned flush logs `shutdown.flush_abandoned`.

**Limits and jobs.** `TOKEN_LAST_USED_PENDING_MAX` and `TOKEN_LAST_USED_FLUSH_INTERVAL_MS` are LIMITS members with 02 rows and `limits.policy.unit` rows owned by `auth/tokens/last-used.ts` (D06-48). There is no job, scheduled or manual: `last_used_flush` stays in `JOB_TYPES` only as a reserved value, because removing it would narrow the v0.2.0 `JobType` wire enum, never joins `MAINTENANCE_JOB_TYPES` and has no schedule or handler; the runnable job lists of `iridium jobs run` and `POST /admin/jobs/:type/run` are exactly `MAINTENANCE_JOB_TYPES`. `oauth_consents.last_used_at` is one of `NON_BUMP_COLUMNS`.

Rejected: a scheduled or manually triggered job, which cannot reach another process's buffer and starves under `JOBS_ENABLED=false`; a swap-and-merge buffer, which can double the bound; module-literal bounds, which the single-source guard refuses; an unnamed warning, which no SIEM rule can match; a drain-phase final flush, which could precede the last admitted requests.

Verification: `tokens.last-used.unit` (`apps/server/src/auth/tokens/last-used.unit.spec.ts`: the bound, latest-wins, the early flush and its backoff, snapshot deletion that keeps a newer observation, failure without re-merge, the final flush at close, and the consent advanced before the token), `access-log.integration` and `metrics.integration`.

Source: D06-49 in [the decision log](../plan/13-decision-log.md) and in [06-mcp-and-agent-access.md](../plan/06-mcp-and-agent-access.md), "Decisions made in this section".
