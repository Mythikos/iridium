# Desktop distribution

*Stub seeded at M0. Not yet written — content lands with the milestone named below, drawn
from `docs/plan/11-operations-and-deployment.md`, not invented ahead of it.*

## What this document will contain

The per-operating-system first-launch procedure for the six unsigned desktop bundles: the Windows SmartScreen and **Unblock** checkbox path, the macOS Gatekeeper and System Settings → Privacy & Security → Open Anyway path, the Linux `iridium.desktop` and `chrome-sandbox` steps, the checksum-verification commands run against the published `SHA256SUMS`, the managed-fleet guidance (`desktop_update_policy.mode = 'disabled'`), the enterprise CA trust guidance from spike S7, the Linux secret-storage caveat, and a plain statement of what shipping unsigned bundles costs at 1.0.

## Source

- docs/plan/11-operations-and-deployment.md, "First-launch friction is expected and is documented, not worked around."
- docs/plan/12-milestones.md §9 (M5 scope, new at M5)
