# M1 verification record

M1 review corrections and implementation are complete locally. The final58 build, both database aggregates, authenticated API fuzzing, database properties, PR chaos, Windows and native Linux desktop, Compose, current 55-migration fixture and fresh full-scope mutation proofs pass. Independent review verifies all 314 selected nightly durability test executions across both engines and the 74.25% mutation score with zero cache reuse. Two original reporting-wrapper failures remain preserved, with their completed workloads separately validated. Final affected checks are recorded below. Formal M0/M1 exit still requires the external prerequisites at the end of this record. `CURRENT` remains `M0`, and no release tag is claimed.

## Status and provenance

M0's local validation record, [M0-exit.md](M0-exit.md), has been rechecked through workspace, real database, container and Windows/Linux desktop validation. The current58 Windows and fresh native Linux proofs are recorded in [Final58 local verification](#final58-local-verification). Its green remote CI and `v0.0.0` tag prerequisites remain unresolved; the owner has deferred branch protection under the direct-main rule. Current remote attempts and repairs are recorded in [remote-ci.md](remote-ci.md).

M1 implements the authenticated headless note kernel: one authorization core, transactional audit, vaults and notes, durable collaborative saving, live revocation and operator commands. The proofs below use real MySQL, product clients, separate processes and production containers.

The historical 53h production image is `iridium-server:m1-53h`, ID `sha256:0822e5cef59e318b41bbf2495b396e5df230c987834463286dd084088bcdb4c4`. Its build provenance is in `reports/m1-final53h-docker-inputs.json` and `m1-final53h-full-host-build-inputs.json`. Source and all 435 host artifacts stayed unchanged through the image build. The 53h corrections isolated each in-process test server environment, preserved independent API response records, documented two existing refusal statuses and explicitly enabled Chromium sandboxing in the Electron test launcher.

## Independent review corrections

The implementation and review fixes were committed and pushed in `29155191d4c116db1897fc083d9939e3ce588f30` (`feat: land the M1 kernel`), including the S01/S02/S14/S06 spike-harness deletions. The following results are targeted historical review evidence; they do not replace the final frozen build, aggregate coverage, mutation, database matrix, or formal exit prerequisites.

| Review finding | Correction and observed boundary |
|---|---|
| Lost committed write acknowledgement | The writer retains the exact submitted batch. Under the current owner fence and head lock, reconciliation requires every durable update byte and attribution field to match; unrelated head changes remain terminal. The real database regression commits before dropping the application transaction result while the process and lease remain alive. This simulates the ambiguous outcome at the store/writer boundary, not a captured MySQL wire-packet loss. |
| Pre-authentication awareness growth | The complete routing key is validated before retention; each physical socket has at most 100 expiring awareness windows and one cleanup timer. Authentication failure cannot reset their quota. Real socket regressions pass on both engines. |
| Skipped grants reported as applied | Per-table grant provenance is durable. Readiness independently probes the critical app privileges in a rolled-back transaction, retries failed probes after DBA repair, and reports skipped or unknown provenance as unverified. Applying the DBA grant script does not erase historical skip metadata. |
| Test resource contention | Test forks start with `UV_THREADPOOL_SIZE=8` by default and at most four workers, bounded further by CPU affinity. CI and mutation receive the same libuv setting. Three concurrent full unit runs at seed `1789766671354` each pass 2,163 tests with zero retries. The later final56 Linux run passes 2,301 tests under an actual two-CPU quota and affinity, with zero retries. |
| Incomplete acceptance enforcement | Every due name in the generated test inventory must resolve to a selected declaring suite with its documented tag. Every inventory row now has an explicit milestone or documented post-1.0 exclusion. Requirements belonging to later features have explicit owners. Every due M0/M1 suite resolves through the enforced inventory and actual runner selection; the complete M1 guard lane passes at the review56 checkpoint. |
| Cache-carried mutation score | The 53h score remains explicitly historical and reused. The actual final58 campaign starts without an input cache and freshly processes the complete configured scope, scoring 74.25% against the unchanged 70 gate with zero reuse. Independent review reconciles all terminal outcomes and source/configuration integrity. The original wrapper's final-progress-display failure is preserved and corrected by separate reporting-only revalidation. |
| Lock waits lose to statement deadlines | Both serving pools set individual row and metadata lock-wait limits below the statement deadline; scoped locks cannot raise that baseline. Real held-lock tests on both engines return `busy` without discarding the healthy connection. Multiple waits or unrelated stalls may still reach the overall deadline. |
| Quadratic user-agent truncation | Truncation examines at most the retained 255-byte prefix and one code point, preserving Unicode boundaries. A long-header regression bounds the amount inspected rather than relying on a wall-clock threshold. |

Additional corrections retain failed-unload intent and retry it while idle; preserve trusted actor/origin fences for each server edit chunk; reject nested chunk transactions before writing; validate canonical binary state vectors before client state changes; keep the dominance deadline active across reconnect and provider replacement; and log the failed-writer transition once per failure episode. The latent route-policy holes now reject unsafe token-capable session routes and public mutating admin routes at boot, with request-time defenses retained. The property-size test now changes the environment and observes parsing/default behavior. The collaboration chaos tag and NoteSessionRegistry milestone ownership are corrected.

Review54 reports include `reports/m1-review54-focused-{84,97}.{json,log}` (27 integration cases per engine), `m1-review54-unit-load-{a,b,c}.json`, and `m1-review54-new-guards-v2.log` (88 policy-guard and affected unit checks). Later source changes require fresh final validation; these counts identify their own checkpoints only. The first isolated persistence performance attempt failed before measurement because the new version-policy consumer preceded its seed migration/build; `m1-review54-performance84.{json,log}` preserves that failure and is not performance evidence.

The harness dependency addition reuses Kysely 0.29.5, already pinned in the workspace. `reports/m1-review54-kysely-importer-delta.json` records the sole lockfile change (the testkit importer); all package resolutions and snapshots remain unchanged. A frozen offline install validates the updated manifests. The older registry audit is therefore evidence about the same resolved package set, not a byte-identical current lockfile or a fresh registry audit.

The review also exposed a deletion-specific durability defect missed by the original clean finding. A real MySQL transaction held before COMMIT showed `saved` after a pure deletion because the Yjs state vector had not changed (`m1-review55-deletion-before-rerun1.{json,log}`; the earlier attempt failed on a fixture API typo). The corrected v1 protocol requires a fixed SHA-256 delete-set witness beside the state vector. Both local and relayed deletion tests now pass on each engine, including reconnect and the close warning (`m1-review56-deletion{84,97}.{json,log}`). A writer-owned pre-repair checkpoint separately proves that its snapshot matches its durable sequence and refuses a changed live document, including deletion-only divergence.

At the targeted review56 checkpoint, all ten forced host build tasks, the check-types command's 24 tasks (including dependency builds), full lint, all 2,301 Windows unit tests and 361 guards pass. Tests use zero retries and the original contention seed; guards retain ten explicit future non-goal skips. The checkpoint/writer cluster passes 90 tests, baseline/store/hooks 67, client/contracts/CRDT 178, and testkit 311. The corresponding `reports/m1-review56-*` records identify that checkpoint; the separate frozen final56 results follow.

## Final56 checkpoint and final57 follow-up

The frozen final56 build used Node 24.21.0 and ten forced host build tasks with zero cache hits. Production image `iridium-server:m1-56` is `sha256:3fa842f5ef2f6e51cab5946d519d963a8014603ccdfc54bc5badc1014f94440b`. Its manifests record 1,213 source entries and 447 compiled host artifacts. Each completed lane checks its recorded source, artifacts and image identity before and after execution. Evidence: `reports/m1-final56-{full-host-build-inputs,docker-inputs}.json`.

| Historical final56 checkpoint | Observed result | Evidence under `reports/` |
|---|---|---|
| Fresh aggregates | MySQL 8.4: 2,765 passed in 232 files; MySQL 9.7: 464 passed in 80 files. Zero failed or skipped cases. Each engine observes all 130 required OpenAPI operation/status pairs. | `m1-final56-aggregate-{84,97}/`, `m1-final56-aggregate-review.json` |
| Strict coverage | 91.82% lines, 90.83% statements, 85.38% branches, 91.34% functions. All unchanged global/per-file thresholds pass; 38 auth/authz runtime files reach 100% in every metric. | `m1-final56-aggregate-84/coverage/` |
| Database properties | Eleven database cases per engine pass, with zero skips and retries. Randomized properties use configured 200-run budgets and at most 60 model commands where applicable. | `m1-final56-lanes-properties/` |
| Two-CPU Linux units | 2,301 tests in 152 files pass with actual quota/affinity 2, two workers, libuv pool 8 and zero retries. Source and installed dependency payloads stay unchanged. | `m1-linux-56-unit-2cpu-r1/evidence/` |
| Windows Electron | Four actual setup/Electron checks pass, with no skips, flakes or retries, against the current MySQL/API and unchanged host artifacts. | `m1-final56-electron-windows/` |
| Production Compose | Real bootstrap, two editors and viewer, pure-deletion commit witness, SIGKILL recovery, original document/undo, audit verification and protected metrics pass on the final56 image. | `m1-compose56-proof.json` |
| First final56 PR chaos | MySQL 8.4 has 86 passing cases, two failures and 42 source-controlled nightly skips. All ten 200-connection revocation load cases pass, worst conservative close 496 ms. Both failures are the stale `head + 1` repair assertions; the corrected assertions check contiguous bounded rows, attribution, exact content and audit. This run remains failed. | `m1-final56-lanes-runtime-campaign/` |
| Fresh API fuzz and runtime | MySQL 8.4 passes 3,592 generated cases and production runtime checks with 55 migrations. MySQL 9.7 finds a schema-valid category-creation request that the notes-only route refuses with 422; its runtime step was therefore not started. The overall sequence remains failed. | `m1-final56-lanes-api-runtime/` |
| Release policy regression | All 52 guards pass, including real disposable Git histories, exact tagged-version notes, merged ancestry, Chromium setup, milestone job floors and prerelease image tags. | `m1-final57-release-policy-tests.{json,log}` |

The 8.4 fuzzer retains 1,351 skipped cases, two operations returning only 401/403, seven repeatedly returning 404, schema-rejection warnings for 17 operations and 6/34 stateful links covered. Its authenticated editor proof records 450 protected successes and 39 revocation invalidations. Passing fuzz checks do not imply complete stateful or data coverage.

The final57 source consolidates the notes-only API contract correction, the repair assertion corrections, release policy and normative deletion-witness documentation. The workspace now uses `verifyDepsBeforeRun: error`: commands refuse stale dependencies and require an explicit install, preventing the concurrent implicit installs observed in the first final57 type attempt. A frozen offline repair passes; package resolutions and the lockfile stay unchanged. Before that build, all 17 workspace type-check tasks, the separate scripts type check, full lint, Knip, 908-file formatting, 413 M1 guards plus ten future skips, 947-file boundaries and test-name references pass. All nine generated artifacts reproduce, with the database schema comparison still assigned to its real database lane. Reports use the `m1-final57-*` prefix; the failed initial type/lint logs remain separate from their passing `r1` records. The final57 host and production image build pass with ten executed tasks and zero cache hits. Its new API regression initially failed because it incorrectly assumed a fresh vault has no children; the corrected oracle compares all ordered vault node rows before and after the refused request. The failed run is retained. Against the unchanged final57 production bytes, all 64 API checks and all five repaired-content chaos checks now pass on each MySQL engine with zero failures, skips or retries (`reports/m1-final57-corrected-regressions/`). The only input delta from that build is the recorded test-oracle correction; inputs and the image remain unchanged during those four runs. The API plan now explicitly phases note-only creation at M1 and category creation at M2. The first final58 host build passed, but its cached Docker build failed dependency verification after the source copy invalidated recorded patch state. The corrected recipe caches `pnpm fetch` and performs the explicit offline frozen install after the final source copy, retaining strict dependency verification. Its changed-source cache regression (`reports/m1-final58-docker-cache-source-change*`) and build58-r1 pass; canonical final58 manifests identify the successful source, host artifacts and image. The failed build remains in `reports/m1-final58-build/`. Completed final58 evidence is recorded in the next section; the earlier 53h/56/57 records remain historical.

## Final58 local verification

The frozen build uses Node 24.21.0 and image `iridium-server:m1-58`, ID `sha256:e8425a9780579bbc14ff967fe61c66adfa7cea91d31fdff0b2897cc89a9cfe96`. Build58-r1 executes all ten host build tasks with zero cache hits. Its canonical manifests identify 1,216 source entries and 447 host artifacts and point to `reports/m1-final58-build-r1/run.json`. Completed runtime lanes preserve their recorded source, artifacts, helpers and immutable image ID. The later fixture promotion records its permitted two-file delta below. Evidence: `reports/m1-final58-{full-host-build-inputs,docker-inputs}.json`.

| Gate | Observed result | Evidence under `reports/` |
|---|---|---|
| Database aggregates | MySQL 8.4: 2,767/2,767 tests in 232 files. MySQL 9.7: 465/465 in 80 files. Zero failures, skips, retries, repeats or unhandled errors. Both freshly observe all 130 explicit OpenAPI operation/status pairs plus five default-covered observations. | `m1-final58-aggregate-{84,97}/`, `m1-final58-aggregate-review-r1.json` |
| Strict 8.4 coverage | Lines 91.84%, statements 90.85%, branches 85.38%, functions 91.34%; unchanged global/per-file gates pass. All 38 product auth/authz modules reach 100% in every metric. The 9.7 aggregate collects no coverage. | `m1-final58-aggregate-review-r1.json` |
| Authenticated API fuzz and runtime | Both engines pass the non-admin light profile: 26 shipped operations, 50 examples per operation, all checks/phases. Generated cases 3,226/3,371 pass; protected successes 361/406, logout invalidations 32/71, authentication failure false. Both production runtime checks pass on image 58. | `m1-final58-lanes-api-runtime-r1/`, `m1-final58-fuzz-review.json` |
| Windows Electron | Three actual Electron cases plus account setup pass with zero failures, skips, flakes or retries against real MySQL/API and frozen host artifacts. | `m1-final58-electron-windows-first/` |
| Literal Compose | Eight checks pass: bootstrap, two editors/viewer, deletion-aware Saved, viewer refusals, SIGKILL recovery with original document/undo, audit and protected metrics. Source/artifacts and immutable image ID stay unchanged; cleanup passes. | `m1-compose58-proof-r1-first/run.json` |
| Database properties | Both engines pass all 11 cases in three files: nine randomized properties with configured 200-run budgets and at most 60 model commands where applicable, plus two deterministic database regressions. Shared size `=`, no soak, no failures or skips, and unchanged repository deadlines. Source, artifacts, helpers and image IDs remain stable. | `m1-final58-lanes-property-chaos/{84,97}/property/{run,tests}.json` |
| PR chaos | The whole project passes 88 cases per engine in 14 files, with zero failed cases and 42 source-controlled nightly skips per engine. `IRIDIUM_CHAOS_ITERATIONS=20`; no title filters, exclusions or deadline/retry overrides. Both lane exits and the four-lane sequence pass with stable inputs/images. | `m1-final58-lanes-property-chaos/{84,97}/chaos/{run,tests}.json`, `m1-final58-lanes-property-chaos/run.json` |
| Native Linux workspace | All 57 configured tasks execute without cache hits: ten builds, 17 type checks, 17 lint tasks and 13 workspace test tasks. Fresh native artifacts and source/dependency provenance remain stable. | `m1-linux-electron-58-native-r1/evidence/{build-result,build-summary}.json`, `m1-final58-linux-review.json` |
| Two-CPU Linux units and Electron | The separate complete unit project passes 2,302 tests in 152 files under actual quota and affinity 2, two resolved workers, UV 8 and retry 0, with no skips/repeats. Three Electron cases plus setup pass once on Node 24.21.0/Electron 44.3.0, UID 1000, enabled Chromium sandbox and default-deny seccomp. Owned cleanup passes. | `m1-linux-electron-58-native-r1/evidence/{unit-result,result,host-result}.json`, `m1-final58-linux-review.json` |
| Selected nightly durability, MySQL 8.4 | Actual test execution passes 157/157: four outage variants, three shutdown cases, 50 revocation-load cases and 100 ordering cases. No skips, retries or repeats; original budgets. All 50 load observations retain 200 sockets, 20 notes and 400 ms MySQL latency; maximum conservative revocation bound 513 ms, below 1,000 ms. The original wrapper remains failed solely on two Vitest 5 truncated parameterized titles; independent review and 15 pure validator checks validate the completed evidence. | `m1-final58-nightly-durability-first/84/`, `m1-final58-nightly-durability-first-review.json`, `m1-final58-nightly-r1-validator-checks.json` |
| Selected nightly durability, MySQL 9.7 | The first 9.7 execution passes 157/157 cases and its r1 wrapper passes. All 50 load observations retain 200 sockets, 20 notes and 400 ms MySQL latency; maximum conservative close bound 518 ms, below 1,000 ms. The independent combined review verifies 314/314 actual tests across both engines, with zero failures, skips, retries or repeats, original budgets, both real negative-drain observations per engine and unchanged source/artifacts/images. The original 8.4 wrapper failure remains preserved. | `m1-final58-nightly-durability-r1/97/`, `m1-final58-nightly-durability-combined-review.json` |
| Performance | Authorizer maximum scenario mean 2.153 µs/call against 50 µs, zero lookups. Writer p95 over 200 real commits is 6.182 ms on 8.4 and 6.114 ms on 9.7 with durability setting 1, below 15 ms nominal and the recorded 3× runner tolerance. | `perf/authz-performance-64448.jsonl`, `perf/persistence-performance-{40728,28280}.jsonl` |
| Before-build statics | All 17 workspace type tasks, scripts types, zero-warning lint and formatting pass; 413 guards pass with ten explicit future skips. The later documentation/workflow checks are recorded separately below. | `m1-final58-static-checks.json`, `m1-final58-static-followup.json`, `m1-final58-guards.json` |
| Current upgrade fixture | PASS: current58 backup-role production on 8.4 and separate write-disabled restores on 8.4 and 9.7 each pass the complete two-case file, six actual test executions total. All nine generated artifacts, real schema comparison and fixture-size checks pass. The 55-migration fixture was generated at `2026-09-19T10:32:11.223Z` and promoted at `2026-09-19T10:34:12Z`: 19,024 bytes; dump SHA256 `d1922c1197a3b4593a42aa5fb7aadf2f5eae2b1b3fd367c243251baea84da1d9`; manifest SHA256 `bda3d853c6113488466236eed2d8e847ac4d1058199bbaab92e5881f491bf9a3`. Only dump and manifest changed; the empty attachment placeholder, original/candidate archives, other source, host artifacts and image IDs are preserved. No rollback was needed. | `m1-final58-upgrade-final/run.json`, `m1-final58-upgrade-review.json` |
| Fresh full mutation | PASS: 74.2451473759885% against the unchanged 70 gate, mutation-process exit 0, zero reused results and no input cache. All 87 configured sources are instrumented into 10,612 unique terminal mutants. The dry run passes 2,034 actual cases in 132 related files. Stryker finishes in 137 min 23 s. All 1,663 source/build input entries and effective settings remain unchanged during execution. Original wrapper failure is preserved; corrected reporting-only provenance and independent review pass. | `m1-mutation-58-full/{run,provenance,provenance-r1}.json`, `m1-final58-mutation-independent-review.json`, `m1-mutation-58-completion-probes-r1.json` |
| Final affected statics | PASS: both workflow YAML files parse and their only semantic changes are mutation job limits from 180 to 360 minutes. Formatting passes both files; all ten focused mutation-lane guards pass with zero skips or retries. Documentation references resolve against all 464 acceptance-map keys, and whitespace checks pass. Final document hashes and the complete source/artifact comparison are recorded separately after this status-row update. | `m1-final58-final-checks.json`, `m1-final58-final-document-check.json`, `m1-final58-local-summary.json` |

The 8.4 aggregate selected `component` but collected **zero files and zero tests**. No component spec is due by M1; neither that selection nor the three selected nightly chaos suites establishes Vitest component execution. The 2,302 aggregate unit tests and the fresh native Linux 2,302-test run are separate executions, not extra cases to add to either aggregate total.

The native Linux proof freshly builds the current sealed 1,161-file checkout at commit `1e13801c60e5da8d901878e18ef7028f77adca31`; its 1,216 original input entries include 55 deleted paths. It reuses verified pinned native dependencies and performs a real frozen offline install in a private copy. Exactly three installation-metadata files and the reviewed generated esbuild launcher change; package binaries, original donor/store and the post-install baseline remain stable. This is not a clean dependency installation. The separate official Electron archive download matches the locked package checksum. The companion product image identifies the shared source but is not used as the native runtime. Chromium sandbox launch is enabled; no independent kernel-level sandbox measurement is claimed. This local Linux proof is not remote CI.

The selected nightly 8.4 campaign remains an original reporting failure. Its runtime tests all passed; pinned Vitest 5 truncates two `$name` display titles to 40 characters, while the wrapper expected the complete labels. The independent review preserves those original reports and checks the complete case inventory, both real negative-drain observations, immutable source/artifacts/images and all load timings. Fifteen pure positive/adversarial validator checks verify the corrected reporting rule without rerunning 8.4 tests. MySQL 9.7 was not started by the failed first campaign; its separate r1 execution and wrapper both pass. The combined review verifies 314 actual passing cases with zero retries or repeats and maximum conservative load-close bounds of 513/518 ms, pairing that first 9.7 execution with the preserved, independently validated 8.4 test evidence. These three suites are bounded nightly durability evidence, not a full nightly or nightly-health history.

Fuzzing retains 1,083/1,218 skipped cases. One operation per engine returns only 401/403; 6/7 repeatedly return 404; 18/17 have schema-rejection warnings. Passing bounded light checks does not establish exhaustive authentication, stateful-link or generated-data coverage. These observations remain part of the passing result.

The current58 fixture was promoted at 10:34:12 UTC after the completed runtime and nightly proofs. The fresh mutation process ran from 10:38:37 to 12:56:01 UTC with the promoted dump and manifest hashes in its before-input capture. Earlier runtime proofs retain their original frozen inputs; the fixture-only delta is recorded separately. Independent mutation review confirms unchanged source/artifacts, helpers, installed tools and the historical default cache. Its newly written dedicated cache is an output of this run, never an input.

The fresh mutation inventory contains 3,848 killed, 283 timed out, 1,269 survived, 164 uncovered, 4,526 compile-error, 521 ignored and one runtime-error result. The 5,401 test-runner outcomes are distinct from compile-error, uncovered and ignored results; all 1,269 survivors completed nonzero tests. Standard scoring uses 4,131 detected out of 5,564 valid mutants. Mutant 1091 (`auth/credentials/throttle.ts`, `tableCreated: true` to `false`) hit a test-runner error while formatting an opaque thrown value; Stryker made its two built-in restart attempts. It is excluded from the standard score, never counted as killed. Conservatively counting it as undetected yields 74.2318%, still above 70. Vitest test retry remains zero; that does not mean Stryker never restarted a worker.

The 2,034 actual dry-run cases map to 2,020 unique test definitions in the mutation report. Six repeated parameterized-title groups in three unchanged files explain the 14-name difference: coverage counters accumulate under shared IDs, and the runner selects every matching case in each group. The exact dependency walk accounts for all 132 selected files and the 20 excluded unit files, which have no runtime path to a mutation target. The mutation leaf uses its pinned Vitest 4.1.11; the aggregate uses Vitest 5.0.0.

The original mutation verifier expected the final periodic progress display to equal 10,091/10,091. Stryker's append-only reporter stops its interval without a final render, so the preserved last display is 10,090/10,091 even though the executor awaits every result, writes the report/cache, accepts the score and logs completion. The versioned r1 verifier retains all scope/configuration/source checks and requires unique terminal inventory, successful process exit, ordered final score/completion events, consistent report/cache timestamps and contents, bounded monotone progress, unchanged original helpers and no reporter errors. All 24 pure positive/adversarial completion checks pass. No mutation workload was rerun or substituted.

After that completed frozen campaign, only four status documents and the two workflow mutation job limits are changed. The workflow edit changes no mutation source, command, scope, property/test budget, concurrency or threshold. Final hashes reconcile this six-file delta from mutation, plus the separately verified two-file fixture promotion from build58. All 447 compiled artifacts, dependency inputs, versions and `CURRENT` remain unchanged; details follow in the final summary.

### Retained failed attempts

The original 58 Docker build failed patch-state verification; the corrected fetch/source-copy/offline-install recipe passes the changed-source cache regression and build58-r1. The first chain failed Windows PATH handling before tests. The first native 58 attempt completed offline installation but rejected a regenerated esbuild launcher before build/tests; a bounded launcher diagnostic preceded the successful native 58-r1 build/unit/Electron proof. The first Compose attempt completed all eight functional checks but rejected an added RepoDigests alias; corrected r1 re-executes and passes while preserving immutable-ID checks. The first selected nightly wrapper failure remains failed, with its passing 8.4 runtime evidence independently validated as described above. The original fresh-mutation wrapper also remains failed on its last periodic display sample; separate terminal-result validation proves the workload completed. Final56 API/chaos failures and historical 53g aggregate failures remain failed records.

## Historical 53h local evidence

| Evidence family | Observed result and boundary | Reports |
|---|---|---|
| Windows workspace | All ten forced build tasks pass across 20 scoped packages, with no cached tasks | `m1-final53h-workspace-build-final.log` |
| Native Linux workspace | Frozen install and standard build/type/lint/test pass in a sealed 1,113-file checkout at commit `0f77bb696007b8b4192649a1079ad3c25b624f64`. The separate root unit/guard selection passed 2,237 tests in 156 files, with ten future skips; the component project was selected but contained zero files; source and original worktree stay unchanged | `m1-linux-53h-final/input/manifest.json`, `m1-linux-53h-final/evidence/` |
| Windows Electron | Four real checks pass with zero retries: account bootstrap, renderer security, HTTP/WS and HTTPS/WSS Origin after navigation/reload. All 1,603 recorded inputs stay unchanged | `m1-final53h-electron-windows.log`, corresponding before/after input manifests |
| Production boot and volumes | Both engines pass non-root UID/GID 10001, fresh volume mode 0750, read-only root, documented writable mounts, ownership readiness and 53 migrations | `m1-final53d-runtime-{84,97}.log` |
| Literal Compose proof | Fresh volumes and CLI bootstrap; two editors converge; viewer writes are refused; SIGKILL/restart retains saved and pending content, original document and undo; fresh viewer, audit and protected metrics pass | `m1-compose53h-proof.json` |
| Provider/socket lifecycle | 102 installed regressions pass across provider lifetime, handshake, NoteSession, NoteClient and real TCP cleanup. Staged ESM and CJS each pass the 42 provider cases | `m1-installed53d-reconnect-fixtures.log`, `m1-provider-patch-staging-verification.json` |
| Database outage recovery | All four variants pass on each engine with all 180 pending markers, original document/undo, bounded pools, no false acknowledgement and unchanged recovery deadlines | `m1-final53d-db-outage-{84,97}.log` |
| Properties and PR chaos | Eleven database cases per engine pass; randomized properties use the explicit PR budget of 200 runs and at most 60 model commands where applicable. PR chaos passes 88 cases per engine | `m1-final53d-properties-{84,97}.log`, `m1-final53d-chaos-pr-{84,97}.log` |
| Revocation stress | Both frozen 53d campaigns pass 150/150: 50 load iterations plus 100 authorization-ordering races per engine. Each load retains 200 sockets, 20 notes and 400 ms MySQL latency; worst conservative close bounds are 534/540 ms, below 1 second | `m1-ch14-53d-nightly-summary.json` |
| Corrected shutdown | Three real Linux cases pass per engine: clean drain/recovery; live-owner row-lock expiry with exit 1 after 20–21 seconds; ownership loss with prompt exit 1. Failed drains retain undrained IDs without false acknowledgement or success logs | `m1-shutdown53f-final-{84,97}.log` |
| Authentication, authorization and CLI | All 38 auth/authz runtime files reach 100% in every metric in the final 8.4 coverage run. Both engines verify CLI owner-executed revocation, rollback and durable result recovery | `m1-final53h-aggregate-{84,97}/`, `m1-final53h-aggregate-review.json` |
| Grants and stored fixture | Both engines pass exact role grants, shipped-client dump/reload and the existing v0.1.0 artifact's hash/migration restoration. These results do not establish final-build fixture provenance | `m1-fixture-grants-final-{84,97}.log`; completed current-build production is recorded below |
| Authenticated API fuzzing | Both 53g runs pass 26 non-admin M1 operations, 50 examples, all checks and applicable phases: 3,222/3,129 generated cases, 98/69 stateful scenarios, 345/341 protected successes and 76/65 logout invalidations, with separate fixture-auth source IPs | `m1-final53g-schemathesis-{84,97}.log`, XML and auth-network JSON |
| Published API links | Both-engine editor traversals pass note creation, committed Markdown/participants and valid/out-of-range line reads. Fourteen OpenAPI contract cases pass, including two dedicated link tests | `m1-final53g-linked-editor-{84,97}.log`, `m1-final53g-openapi-links-contract.log` |
| Static/generated hygiene | 21 checks pass, including 17 workspace type checks, zero-warning lint, 860-file format check, 267 M1 guards plus ten future skips, Knip/hints, boundaries, licenses, dedupe, dependency instances and env/test inventories. All nine generated artifacts reproduce; subsequent workflow guards and the 451-name acceptance map pass | `m1-final53g-static-summary.json`, `m1-final53g-post-mutation-summary.json`, `m1-final53g-gen-auth-inventory-check.log` |

Report paths in tables are relative to `reports/`. The 53d campaigns remain targeted historical runtime evidence with their original runtime/input manifests; later shutdown and HTTP/schema changes have separately identified proofs. The property result is the explicit PR budget, not a claim of 5,000 nightly runs. Interrupted campaigns and infrastructure failures are preserved and excluded from passing counts.

The fuzz reports retain 1,035/1,023 skipped cases; 2/1 operations returned only 401/403, and 6/7 resource operations repeatedly returned 404. Schema-rejection warnings affected 17/18 operations. Stateful coverage was 7/34 and 5/34 links, with 27 inferred links in each denominator. Those limits remain part of the passing 53g result.

## Historical 53g/53h mutation and coverage

The historical **73.63%** mutation score is carried forward from the 53g cache, above the unchanged 70 threshold. The 53h incremental run passed **1,718 initial tests**, reused 9,667 mutant results and evaluated 31 candidates; the other 521 mutants were ignored. It did not freshly execute all 10,219 mutants. The archived 53d baseline reports 66.56% but lacks the later `run.json`/`provenance.json` chain-root capture. Forced reruns of six targets and the 53g full-scope incremental run provide partial execution provenance, not a fresh full-scope campaign. These records remain historical: `reports/m1-mutation53d-baseline/`, `m1-mutation-53g-forced/`, `m1-mutation-53g-full/` and `m1-mutation-53h-full/{run,provenance}.json`. The final58 campaign above now provides the fresh forced full-scope proof after the review corrections, with an absent input cache and zero reused results. Its newly written incremental file is an output artifact, not an input to that proof.

At the historical checkpoint, the CI mutation limit was raised to match nightly's 180 minutes. After observing the fresh final58 Stryker duration of 137 min 23 s, both mutation job limits are now 360 minutes to allow installation, build and slower-runner overhead. Remote mutation timing has not been measured. Full scope, concurrency 4, 200 property runs, seed 42, test/property deadlines, retry 0 and threshold 70 remain unchanged. Reused results are not described as freshly executed mutants.

The final 53h 8.4 aggregate passed 2,372 tests with zero failures or skips and the unchanged global/per-file M1 coverage gate: 91.09% lines, 90.07% statements, 84.10% branches and 90.47% functions. All 38 auth/authz files reach 100% in every metric. The 9.7 integration/contract aggregate passed 402 tests with zero failures or skips. Both runs freshly cover all 130 explicit OpenAPI operation/status pairs; the additional auth.createSession 503 matches its default schema. Source and image identities remain unchanged. Evidence: `reports/m1-final53h-aggregate-{84,97}/`, `reports/m1-final53h-aggregate-review.json`.

## Historical 53h current-build checks

| Gate | Completed result |
|---|---|
| Linux Electron | PASS: four expected cases, zero failures/flaky/skips, real MySQL/API and account setup, enabled Chromium sandbox, HTTP/WS and HTTPS/WSS Origin across navigation/reload. All 1,113 sealed source files and build artifacts unchanged; owned resources cleaned up. Evidence: `reports/m1-linux-electron-53h-sandbox/evidence/` |
| Current-build v0.1.0 fixture | PASS: whole-file backup-role production on 8.4 and separate write-disabled whole-file restores on 8.4 and 9.7, two tests each. Fixture-size and all nine generated-artifact checks pass. Candidate hashes stayed stable; only the two expected fixture files changed. Evidence: `reports/m1-final53g-upgrade-53h-rerun1/run.json` |
| Affected final checks | The 53h source passed all 17 type tasks, zero-warning lint, formatting, 267 guards plus ten future skips, all nine generators, boundaries, Knip and scripts types. The final fixture-only checks also pass; evidence: `reports/m1-final53h-local-summary.json` |

Both 53g final aggregates failed their hidden-metrics owner-lifecycle case and fresh OpenAPI coverage check; their `run.json` files record nonzero test and inventory exits. They remain failures. A passing recorder-writer test or the 14 link/contract cases cannot substitute for `scripts/check-openapi-coverage.ts` against fresh records.

D12-4 current-build production was fulfilled for the historical 53h build; the current 55-migration fixture outcome is recorded in the final58 table. The historical 53h promoted fixture was generated at `2026-09-18T06:41:33.995Z` with dump SHA256 `b4f7e7c46d9e46cd4cd380f746188b2a40ef051606e18815d9540f72a0484f17`. It uses the backup role, 53 migrations and the production image's shipped MySQL client. An earlier reporting-wrapper failure followed a passing producer; that attempt archived the candidate and restored the original. The corrected complete sequence passed before promotion. Both originals and candidates remain archived.

The Linux desktop proof runs in an owned non-root container with a read-only root, no added capabilities and no-new-privileges. Its derived default-deny seccomp profile adds only `chroot` for Chromium's user-namespace sandbox; the original profile and semantic delta are preserved. The original readiness-path omission and later sandbox-denial attempt remain recorded failures. No sandbox-disable flags were added, and no independent native-kernel sandbox measurement is claimed. The 20 recorded Origin observations all equal `app://iridium`; `reports/m1-linux-electron-53h-summary.json` records source/build integrity and verified cleanup.

## Historical 53h retained Schemathesis scope

At the historical 53h checkpoint, a full fuzz repeat was not required solely for documenting `me.sessions.list` 403 and `me.changePassword` 429. Both additions use the same `ProblemDetails` content already accepted through the operations' default response. Removing only those additions recreates the exact 53g OpenAPI SHA256 `f5dedcb537a45f343faa0f057f9d9ccc60b469affb22c2488b82290a214cc9e1`; all other request/response schemas, links and operation selection are unchanged.

The read-only comparison also confirms all 186 non-spec serving source paths, fuzzer sources and recorded dependency inputs match 53g. Durable evidence is `reports/m1-final53h-retained-schemathesis.json`. Retain the original 53g PASS, with its warnings and provenance; do not label it a fresh 53h execution. The final 53h contract validation and fresh operation/status inventory cover both additions.

## Decisions and spike evidence

The changed ADR inventory and its index record these M1 decisions; the authoritative decision log remains `docs/plan/13-decision-log.md`:

| ADR | Recorded decision |
|---|---|
| A17, `0017-hocuspocus-embedded.md` | Embedded Hocuspocus ownership and provider detach ordering |
| A20, `0020-live-role-change.md` | Live role changes and same-document recovery after persistence/content latches clear |
| `d04-14-owner-executed-session-revocation.md` | Serving-owner revocation command, admission/drain barrier and durable outcomes |
| `d05-06-transient-collaboration-failures.md` | Retryable dependency failures preserve the client's live document |
| `d10-33-collaboration-owner-lease.md` | Schema-scoped serving ownership and takeover fencing |
| `d12-20-schemathesis-milestone-scope.md` | M1 non-admin light scope with all checks/phases and 50 examples |
| `ops-12-serving-sql-deadlines.md` | Bounded serving SQL, poisoned-connection disposal and unknown COMMIT outcomes |

All due spike documents carry closed verdicts. `docs/spikes/S10-hocuspocus-clientid-stability.md` records **pass on both engines** with actual continuous compaction, unchanged client identity, whole-vector saving, undo and child restart. The `collab.clientid-stable.chaos` reproduction remains in the required PR chaos lane.

At the historical 53h checkpoint, the database and desktop snapshots preceded final fixture production. That sequence changed only the fixture dump and manifest; the subsequent incremental mutation run included the promoted fixture. Those verification documents were updated after the frozen runs. This ordering describes only the 53h snapshot; the 2026-09-18 review fixes changed runtime and tests, and final58 evidence is recorded separately above.

## Formal exit prerequisites

Remote CI is now running on `main`; its failures and repairs are recorded in [remote-ci.md](remote-ci.md). A complete green required-check run and the predecessor `v0.0.0` tag remain prerequisites. The owner's direct-main rule defers branch protection and forbids version PR automation. A sealed local Linux checkout is not GitHub Actions evidence. Local macOS Electron remains outstanding; current58 Windows and fresh native Linux desktop proofs pass locally, as recorded above.

The first nightly workflow was manually dispatched as run `35475597877`; its failures are retained in [remote-ci.md](remote-ci.md). Scheduled nightly history is still absent. The exit record must distinguish that missing history from the actual dispatch results and identify the required workflow runs.

The M1 changeset is prepared. Versions, `CURRENT` and tags remain unchanged; the exit record and `v0.1.0` tag follow completion of the predecessor and [M1 exit criteria](../plan/12-milestones.md#54-exit-criteria). M5/M6/M8 manual acceptance records and the M8 restore drill do not add an M1 UI or operations-review gate.

Automatic approval review rejected a fresh registry audit because it would transmit dependency metadata without the required authorization. The earlier dated audit passed with no high or critical findings against the same resolved package set (the later testkit Kysely importer addition changes the lockfile bytes); `reports/m1-final53g-audit-retained.json` preserves that result without claiming a fresh audit.
