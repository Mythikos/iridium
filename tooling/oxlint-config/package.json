{
  "name": "@iridium/oxlint-config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared oxlint fragments: base, react, node, server (13-decision-log.md A1)",
  "type": "module",
  "exports": {
    "./base": "./base.ts",
    "./react": "./react.ts",
    "./node": "./node.ts",
    "./server": "./server.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit -p tsconfig.json",
    "lint": "oxlint -c ../../oxlint.config.ts ."
  },
  "devDependencies": {
    "@iridium/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "oxlint": "catalog:"
  }
}
